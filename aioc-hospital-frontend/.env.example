# Backend URLs — the browser will call these.
# For local dev (npm run dev): copy this file to .env or .env.local so the app
# can reach the backends (e.g. patient list from management, calendar from scheduling).
# For Docker: these are passed as build args from the repo root .env.
VITE_API_URL=http://localhost:8000
VITE_MANAGEMENT_API_URL=http://localhost:8001
VITE_SCHEDULING_API_URL=http://localhost:8002
VITE_REPORTS_API_URL=http://localhost:8003
